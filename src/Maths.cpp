#include "Maths.h"

// void Log(const Vec4& vec) {
//     LogInfo("%f, %f, %f, %f", vec.x, vec.y, vec.z, vec.w);
// }
// void Log(const Mat4& mat) {
//     for (int i = 0; i < 4; i++) {
//         const VecType* d = &mat.Buf[i][0];
//         LogInfo("%f, %f, %f, %f%s", d[0], d[1], d[2], d[3], (i==3)?"\n":"");
//     }
// }

